### Instructions for Setting Twitter
Copy `configsample.yaml` to `config.yaml`.

Update config.yaml to include appropriate Twitter keys.

Update screen_names.csv to include companies you want to follow.
